export interface IUserInfo {
  id: number,
  name: string,
  realname: string,
  cellphone: string,
  enable: number,
  departmentId: number,
  roleId: number,
  createAt: string,
  updateAt: string
}
